# 领域约定

社区武学传习风控平台围绕教练凭据、参与者限制和课程安全放行保存业务事实。主要对象为认证机构、教练凭据、合作单位、课程、参与者声明、监护同意、紧急访问。

必须长期遵守以下约束：排课确认时冻结资格判定；凭据撤销阻止新课并提示旧课；敏感健康信息按职责披露；紧急访问必须限时并复核。

时间以带时区的 ISO 8601 字符串交换，服务内部保存 UTC；外部写请求使用调用方提供的幂等键；审计记录采用追加方式保存，不通过覆盖来隐藏历史。